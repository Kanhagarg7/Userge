user:
  name: kanhagarg7
  github: kanhagarg7 
language: "python"
python_version: "latest"

commands:
  build:
    - "pip install -r requirements.txt"
  package:
    - "ffmpeg build-essential"
  start:
    - "python3 -m userge"
  env:
    - "ENVIRONMENT=production"
